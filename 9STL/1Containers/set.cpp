// Learning About Set in CPP
//stores unique elements only

#include <iostream>
#include<set>
using namespace std;

int main()
{
    //creation of set
    set<int> st;

    //insertion in set
    st.insert(10);
    st.insert(15);
    st.insert(8);
    st.insert(4);

    return 0;
}